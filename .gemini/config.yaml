# @format

# yaml-language-server: $schema=./config.schema.json
code_review:
  disable: false
  comment_severity_threshold: LOW
  max_review_comments: -1
  pull_request_opened:
    code_review: true
    help: true
    summary: true
    include_drafts: true
have_fun: true
ignore_patterns: []

memory_config:
  disabled: false
